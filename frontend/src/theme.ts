import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#1a1a1a',
    },
    secondary: {
      main: '#1a8cff',
    },
    background: {
      default: '#262626',
    }
  },
});

export default theme;
