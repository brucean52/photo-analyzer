{
  "name": "photo-analyzer",
  "version": "1.0.0",
  "description": "App requires a google vision api json key in order to work.",
  "main": "index.js",
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\""
  },
  "author": "Bruce An",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
