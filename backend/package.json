{
  "name": "photo-analyzer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "prod": "node dist/bundle.cjs",
    "build": "esbuild index.js --bundle --minify --platform=node --target=node20 --outfile=dist/bundle.cjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bruce An",
  "license": "ISC",
  "description": "",
  "type": "module",
  "dependencies": {
    "@google-cloud/vision": "^4.3.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "http-status-codes": "^2.3.0",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "nodemon": "^3.1.4"
  }
}
